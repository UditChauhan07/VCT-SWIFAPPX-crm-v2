export const fields = {
    address_label: {
      type: 'string',
      label:  'address_label',
      displayLabels: ['name'],
      // disableForTable: true,
    },
    contact_person: {
      type: 'string',
      label:  'contact_person',
      displayLabels: ['name'],
      // disableForTable: true,
    },
    contact_number: {
      type: 'string',
      label:  'contact_number',
      displayLabels: ['name'],
      // disableForTable: true,
    },
    street: {
      type: 'string',
      label:  'street',
      displayLabels: ['name'],
      // disableForTable: true,
    },
    unit: {
      type: 'string',
      label:  'unit',
      displayLabels: ['name'],
      // disableForTable: true,
    },
    block: {
      type: 'string',
      label:  'block',
      displayLabels: ['name'],
      disableForTable: true,
    },
    state: {
      type: 'string',
      label:  'state',
      displayLabels: ['name'],
      disableForTable: true,
    },
    country: {
      type: 'string',
      label:  'country',
      displayLabels: ['name'],
      // disableForTable: true,
    },
    postal_code: {
      type: 'string',
      label:  'postal_code',
      displayLabels: ['name'],
      // disableForTable: true,
    },

   
  };
  
  export const readColumns = {
   
    address_label: {
      type: 'string',
      disableForForm: true,
    },

    contact_person: {
      type: 'string',
      disableForForm: true,
    },

    contact_number: {
      type: 'string',
      disableForForm: true,
    },

    address_label: {
      type: 'string',
      disableForForm: true,
    },
  };
  